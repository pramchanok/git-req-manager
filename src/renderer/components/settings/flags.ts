// ── Feature flags ──
// ซ่อน UI ชั่วคราว (โค้ดเบื้องหลังยังทำงานครบ) — เปลี่ยนเป็น true เพื่อเปิดกลับมา
// ตอนนี้ใช้ Custom Webhook URL (relay server) เป็นหลัก จึงไม่ต้องแสดง Tunnel/Polling
export const SHOW_TUNNEL_OPTION = false
export const SHOW_REFRESH_INTERVAL = false
